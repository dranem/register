{% extends 'AcmeAccountBundle:Account:base.html.twig' %}
{% block body %}
	<h1>Update Password</h1>
	{% for flashMessage in app.session.flashbag.get('notice') %}
	<div class="flash-notice">
	    {{ flashMessage }}
	</div>
	{% endfor %}
	{{ form(form) }}
	<a href ="{{ path('account_home') }}">Back</a>
{% endblock %}